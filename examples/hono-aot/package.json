{
  "name": "hono-aot-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Hono + ata-AOT bundle size demo",
  "scripts": {
    "build:schemas": "node ../../bin/ata.js build 'schemas/*.schema.json' --out-dir build",
    "measure": "node measure.mjs"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "ajv": "^8.18.0",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
